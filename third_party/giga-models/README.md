# giga-models third-party subset

This directory keeps only the generic pieces that the standalone
`giga-world-policy-0.5` project still imports from `giga_models`, such as socket
client/server helpers and path utilities.

Project-owned WAM model, trainer, transform, and dataset code now lives under
`../../world_action_model`.
